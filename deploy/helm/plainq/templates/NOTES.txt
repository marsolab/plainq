PlainQ has been deployed.

Release:  {{ .Release.Name }}
Namespace: {{ .Release.Namespace }}
Storage driver: {{ .Values.storage.driver }}

1. Houston admin web UI + REST auth API + /health + /metrics (HTTP, port {{ .Values.service.httpPort }}):

   kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "plainq.fullname" . }} {{ .Values.service.httpPort }}:{{ .Values.service.httpPort }}

   Then open: http://localhost:{{ .Values.service.httpPort }}/
   Health:    http://localhost:{{ .Values.service.httpPort }}{{ .Values.config.healthRoute }}
   Metrics:   http://localhost:{{ .Values.service.httpPort }}{{ .Values.config.metricsRoute }}

2. gRPC queue API (port {{ .Values.service.grpcPort }}):

   kubectl --namespace {{ .Release.Namespace }} port-forward svc/{{ include "plainq.fullname" . }} {{ .Values.service.grpcPort }}:{{ .Values.service.grpcPort }}

   Point your gRPC client at: localhost:{{ .Values.service.grpcPort }}

{{- if .Values.ingress.enabled }}

   Ingress is enabled. Hosts:
   {{- range .Values.ingress.hosts }}
   - http(s)://{{ .host }}
   {{- end }}
   Note: the gRPC host needs backend-protocol: GRPC on your Ingress controller.
{{- end }}

3. Authentication:
{{- if .Values.auth.enabled }}
   Auth is ENABLED. The JWT signing secret is sourced from Secret
   "{{ include "plainq.jwtSecretName" . }}" (key "{{ .Values.auth.secretKey }}")
{{- if .Values.auth.existingSecret }}
   (pre-existing secret you provided via auth.existingSecret).
{{- else }}
   (generated by this chart from auth.jwtSecret — use auth.existingSecret in production).
{{- end }}
{{- else }}
   Auth is DISABLED. Do not run this configuration in production.
{{- end }}

4. Onboarding: open the Houston UI (step 1) and complete the first-run
   onboarding flow to create the initial admin account, then issue API
   credentials for your gRPC clients.

{{- if eq .Values.storage.driver "sqlite" }}

NOTE: SQLite mode runs a single StatefulSet replica backed by a PersistentVolume.
Do not scale beyond 1 replica. For horizontal scaling switch storage.driver to "postgres".
{{- end }}
